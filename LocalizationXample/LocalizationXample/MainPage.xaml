<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:LocalizationXample"
             x:Class="LocalizationXample.MainPage">
    <Page.BindingContext>
        <local:MainViewModel />
    </Page.BindingContext>
    <StackLayout>
        <!-- Place new controls here -->
    	<Label Text="{x:Static local:Messages.Title}" 
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand" />

        <StackLayout Orientation="Horizontal">

            <Label HorizontalOptions="Start"
                   Text="{x:Static local:Messages.UserLabel}"/>
            <Entry HorizontalOptions="FillAndExpand" Placeholder="{x:Static local:Messages.UserPlaceholder}"/>
            
        </StackLayout>

        <StackLayout Orientation="Horizontal">

            <Label HorizontalOptions="Start"
                   Text="{x:Static local:Messages.PasswordLabel}"/>
            <Entry HorizontalOptions="FillAndExpand" Placeholder="{x:Static local:Messages.PasswordPlaceholder}"/>

        </StackLayout>

        <Button Text="{x:Static local:Messages.ChangeLanguage}" Command="{Binding ChangeCommand}"/>
    </StackLayout>

</ContentPage>
